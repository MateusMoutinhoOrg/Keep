package config

// Version is the release of this library, reported through the
// api.Lib.Version field. It lives in a file of its own so a release bump is a
// one-line edit touching no logic.
const Version = `v0.0.4`
